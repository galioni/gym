# Next Phases (Future Work Only)

This file tracks only upcoming phases and steps. It does not track completed work.

## Current Phase: Phase 5 (In Progress)

## Phase 5: Cloud-Only Sync Mode
1. Deploy the cloud-only sync changes to production.
2. Validate in production that sync settings no longer expose local/cloud mode switching.
3. Validate cloud sync still works end-to-end (sign in -> sync now -> reload persists).
4. Monitor auth/sync API logs during the post-deploy stabilization window.
